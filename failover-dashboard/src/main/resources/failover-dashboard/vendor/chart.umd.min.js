/*
 * PLACEHOLDER — replace with the real Chart.js v4 UMD build (chart.umd.min.js).
 *
 * The dashboard intentionally vendors Chart.js (offline-safe, no CDN, CSP-friendly — design doc §7).
 * The actual minified library is a third-party binary and is NOT checked in here as source; drop the
 * official build into this path at packaging time (e.g. via the frontend-maven-plugin or a committed
 * vendored copy).
 *
 * Until the real build is present, `window.Chart` stays undefined and app.js degrades gracefully:
 * KPI cards and the per-API health table render normally; the chart canvases are skipped and a
 * non-blocking notice is shown. No remote script is ever loaded.
 *
 * Source: https://www.chartjs.org/  (npm: chart.js@^4 — dist/chart.umd.js)
 */
