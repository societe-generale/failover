/**
 * Read-only data-transfer objects returned by the dashboard REST API. Carry aggregate, non-sensitive
 * configuration and metrics only (design doc §9 data-minimisation).
 */
package com.societegenerale.failover.dashboard.dto;
