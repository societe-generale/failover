/**
 * Configuration properties bound under the {@code failover.*} prefix.
 *
 * <p>{@link com.societegenerale.failover.properties.FailoverProperties} is the root
 * properties bean.  Nested property classes cover store ({@code failover.store.*}),
 * scheduler ({@code failover.scheduler.*}), scatter/gather ({@code failover.scatter.*}),
 * and multi-tenant ({@code failover.store.multitenant.*}) settings.
 */
package com.societegenerale.failover.properties;
